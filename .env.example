# .env.example
# This is an example .env file. Rename it to .env and fill in your actual configuration.

PORT=3000 # Port for the server to listen on, default is 3000
API_KEY=YOUR_API_KEY # API key to secure your backend endpoints, generate a strong random key
VIDEOS_DIR=./uploads/videos # Directory to store uploaded videos, default is ./uploads/videos relative to the server.js

# Database Configuration - MySQL
DB_HOST=YOUR_DB_HOST # Hostname or IP address of your MySQL database server
DB_PORT=3306 # Port number of your MySQL database server, default is 3306
DB_NAME=YOUR_DB_NAME # Name of the database to use for Vidplo
DB_USER=YOUR_DB_USER # Username for database access
DB_PASSWORD=YOUR_DB_PASSWORD # Password for database access

# CORS Configuration - Comma separated origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourfrontenddomain.com # Comma-separated list of allowed origins for CORS

# Backend Server URL - for metadata service (if applicable)
BACKEND_SERVER_URL=http://your-metadata-service.com # URL of your backend metadata service, if you are using one.

# Optional: Delete API Key - for deleting thumbnails (secure this carefully if used)
# DELETE_API_KEY=YOUR_DELETE_API_KEY # API key for DELETE thumbnail endpoint - use only if you need this endpoint and secure it.

# Example for production environment you might want to set specific origins:
# CORS_ALLOWED_ORIGINS=https://vidplo.yourdomain.com,https://admin.yourdomain.com